/* import { Router } from "express";
import fs from 'fs';

const router = Router();

router.post('/', async(req,res)=>{
    const productos = req.body
    let productos = []
    if(fs .existsSync(path){
        const 
    })
}) */